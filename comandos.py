#<h1>(no começo e </h1> no final) sinaliza o titulo do site
#para passar um for é necessario utilizar : {% (seu for) %}, depois é necessario : {%endfor%
# para passar uma variavel no html é necessario usar {{ (sua variavel) }}
# <form action = "/(rota predefinida)" method ="post"> serve para passar informações
#methods=['METODO',] utilizamos no @approute para dizer se a pagina recebe get ou post
# session[''] = request.form['objeto referenciado'] serve para armazenar o login nos cookies do navegador
#flash() 